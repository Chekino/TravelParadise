{% if visites is empty %}
    <div class="alert alert-light border text-center">Aucune visite à afficher.</div>
{% else %}
    <div class="table-responsive mt-3">
        <table class="table table-bordered table-hover align-middle">
            <thead class="table-success text-center">
                <tr>
                    <th>#</th>
                    <th>Titre</th>
                    <th>Lieu</th>
                    <th>Date</th>
                    <th>Heure</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                {% for visite in visites %}
                    <tr>
                        <td>{{ loop.index }}</td>
                        <td><strong>{{ visite.titre }}</strong></td>
                        <td><span class="badge bg-success-subtle text-dark">{{ visite.lieu }}</span></td>
                        <td>{{ visite.date|date('d/m/Y') }}</td>
                        <td>{{ visite.heureDebut|date('H:i') }}</td>
                        <td>
                            <a href="{{ path('guide_visite_detail', {id: visite.id}) }}" class="btn btn-outline-success btn-sm">
                                📄 Infos
                            </a>
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
{% endif %}

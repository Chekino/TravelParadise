{% if visites is empty %}
    <div class="alert alert-info">Aucune visite à afficher.</div>
{% else %}
    <div class="table-responsive">
        <table class="table table-hover table-bordered align-middle">
            <thead class="table-light">
                <tr>
                    <th>#</th>
                    <th>Titre</th>
                    <th>Lieu</th>
                    <th>Date</th>
                    <th>Heure</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                {% for visite in visites %}
                    <tr>
                        <td>{{ loop.index }}</td>
                        <td><strong>{{ visite.titre }}</strong></td>
                        <td>
                            <span class="badge bg-secondary">{{ visite.lieu }}</span>
                        </td>
                        <td>
                            <span class="badge bg-light text-dark">
                                {{ visite.date|date('d/m/Y') }}
                            </span>
                        </td>
                        <td>{{ visite.heureDebut|date('H:i') }}</td>
                        <td>
                            <a href="{{ path('guide_visite_detail', {id: visite.id}) }}"
                               class="btn btn-sm btn-outline-primary">
                                📄 Détails
                            </a>
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
{% endif %}
